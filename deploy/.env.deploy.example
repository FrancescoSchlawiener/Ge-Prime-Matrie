# Copy to deploy/.env.deploy (gitignored)
VPS_HOST=145.223.116.113
VPS_USER=root
VPS_SSH_CONFIG=deploy/ssh.config.local
APP_DIR=/opt/ge-prime-matrix
SITE_DIR=/var/www/schlawiener
BRANCH=cursor/hostinger-vps-live-pipe-95cf

# Existing docker publish ports (check with: docker ps)
PROTOSARCHE_PORT=8081
CATPOCALYPSE_PORT=8082
